const authUser = require('./authUser');
const authUserOptional = require('./authUserOptional');
const linkPostExists = require('./linkPostExists')
const userExists = require('./userExists');

module.exports = {
    authUser,
    authUserOptional,
    linkPostExists,
    userExists,
};
